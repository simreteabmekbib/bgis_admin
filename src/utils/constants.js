const ACCESS_TOKEN_LIFETIME = 60 * 15 * 1000; // mili-seconds
const REFRESH_TOKEN_LIFETIME = 2 * 30 * 24 * 60 * 60 * 1000; // mili-seconds
const ACCESS_TOKEN_NAME = 'accessToken';
const REFRESH_TOKEN_NAME = 'refreshToken';

export{
    ACCESS_TOKEN_LIFETIME,
    REFRESH_TOKEN_LIFETIME,
    ACCESS_TOKEN_NAME,
    REFRESH_TOKEN_NAME
}